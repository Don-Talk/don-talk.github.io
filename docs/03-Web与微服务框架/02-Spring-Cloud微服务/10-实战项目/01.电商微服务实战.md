# Spring Cloud微服务实战项目 - 电商系统

## 📋 项目概述

### 项目背景

本实战项目将带你从零开始,构建一个完整的电商微服务系统,涵盖用户管理、商品管理、订单管理、支付管理等核心业务模块,并集成Spring Cloud全家桶的所有核心技术。

### 技术架构

```
┌─────────────────────────────────────────────────────┐
│                  前端应用层                          │
│  Web端 / 移动端 / 小程序                             │
└────────────────────┬────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────┐
│              API Gateway (8080)                      │
│         路由转发 / 鉴权 / 限流                       │
└──┬────────┬────────┬────────┬──────────────────────┘
   │        │        │        │
┌──▼──┐ ┌──▼──┐ ┌──▼──┐ ┌──▼──────────┐
│用户  │ │商品  │ │订单  │ │支付         │
│8081 │ │8082 │ │8083 │ │8084         │
└──┬──┘ └──┬──┘ └──┬──┘ └──┬──────────┘
   │        │        │        │
   └────────┴────────┴────────┘
            │
    ┌───────▼────────┐
    │  Nacos集群      │
    │  注册中心+配置  │
    └────────────────┘
            │
    ┌───────▼────────┐
    │  MySQL + Redis  │
    │  RabbitMQ       │
    └────────────────┘
```

---

## 🏗️ 项目结构

### Maven多模块结构

```
mall-microservice/
├── mall-common/              # 公共模块
│   ├── mall-common-core/     # 核心工具类
│   ├── mall-common-api/      # 通用API定义
│   └── mall-common-redis/    # Redis封装
│
├── mall-gateway/             # API网关
│
├── mall-service/             # 业务服务
│   ├── mall-user-service/    # 用户服务
│   ├── mall-product-service/ # 商品服务
│   ├── mall-order-service/   # 订单服务
│   └── mall-pay-service/     # 支付服务
│
├── mall-infra/               # 基础设施
│   ├── mall-nacos/           # Nacos配置
│   └── mall-sentinel/        # Sentinel规则
│
└── pom.xml                   # 父POM
```

---

## 🔧 环境准备

### 软件版本

| 软件 | 版本 | 说明 |
|------|------|------|
| JDK | 1.8+ | Java开发工具包 |
| Maven | 3.6+ | 项目构建工具 |
| MySQL | 8.0+ | 关系型数据库 |
| Redis | 7.0+ | 缓存数据库 |
| RabbitMQ | 3.9+ | 消息队列 |
| Nacos | 2.2+ | 注册中心+配置中心 |
| Docker | 20.10+ | 容器化部署 |

### Docker快速搭建环境

```bash
# 创建docker-compose.yml
version: '3'
services:
  # MySQL
  mysql:
    image: mysql:8.0
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: mall
    ports:
      - "3306:3306"
    volumes:
      - ./data/mysql:/var/lib/mysql
      
  # Redis
  redis:
    image: redis:7.0
    container_name: redis
    ports:
      - "6379:6379"
      
  # RabbitMQ
  rabbitmq:
    image: rabbitmq:management
    container_name: rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
      
  # Nacos
  nacos:
    image: nacos/nacos-server:latest
    container_name: nacos
    environment:
      MODE: standalone
    ports:
      - "8848:8848"
      - "9848:9848"
      
  # Zipkin
  zipkin:
    image: openzipkin/zipkin:latest
    container_name: zipkin
    ports:
      - "9411:9411"

# 启动所有服务
docker-compose up -d

# 查看运行状态
docker-compose ps
```

---

## 📦 第一步: 创建父工程

### pom.xml

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    
    <groupId>com.mall</groupId>
    <artifactId>mall-microservice</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    
    <modules>
        <module>mall-common</module>
        <module>mall-gateway</module>
        <module>mall-service</module>
    </modules>
    
    <properties>
        <java.version>1.8</java.version>
        <spring-boot.version>2.7.14</spring-boot.version>
        <spring-cloud.version>2021.0.8</spring-cloud.version>
        <spring-cloud-alibaba.version>2021.0.5.0</spring-cloud-alibaba.version>
        <mybatis-plus.version>3.5.3.1</mybatis-plus.version>
        <mysql.version>8.0.33</mysql.version>
        <redis.version>2.7.14</redis.version>
    </properties>
    
    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            
            <!-- Spring Cloud -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            
            <!-- Spring Cloud Alibaba -->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring-cloud-alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
```

---

## 🛠️ 第二步: 创建公共模块

### mall-common-core/pom.xml

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.mall</groupId>
        <artifactId>mall-common</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    
    <dependencies>
        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>
        
        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        
        <!-- Jackson -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
    </dependencies>
</project>
```

### 统一返回结果

```java
package com.mall.common.core.result;

import lombok.Data;
import java.io.Serializable;

@Data
public class Result<T> implements Serializable {
    
    private Integer code;
    private String message;
    private T data;
    
    public static <T> Result<T> success() {
        return success(null);
    }
    
    public static <T> Result<T> success(T data) {
        Result<T> result = new Result<>();
        result.setCode(200);
        result.setMessage("success");
        result.setData(data);
        return result;
    }
    
    public static <T> Result<T> error(String message) {
        Result<T> result = new Result<>();
        result.setCode(500);
        result.setMessage(message);
        return result;
    }
    
    public static <T> Result<T> error(Integer code, String message) {
        Result<T> result = new Result<>();
        result.setCode(code);
        result.setMessage(message);
        return result;
    }
}
```

### 全局异常处理

```java
package com.mall.common.core.exception;

import com.mall.common.core.result.Result;
import lombok.extern.slf4j.Slf4j;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;

@Slf4j
@RestControllerAdvice
public class GlobalExceptionHandler {
    
    @ExceptionHandler(BusinessException.class)
    public Result<?> handleBusinessException(BusinessException e) {
        log.warn("业务异常: {}", e.getMessage());
        return Result.error(e.getCode(), e.getMessage());
    }
    
    @ExceptionHandler(Exception.class)
    public Result<?> handleException(Exception e) {
        log.error("系统异常", e);
        return Result.error("系统繁忙,请稍后重试");
    }
}
```

---

## 🚪 第三步: 创建API网关

### mall-gateway/pom.xml

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.mall</groupId>
        <artifactId>mall-microservice</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    
    <artifactId>mall-gateway</artifactId>
    
    <dependencies>
        <!-- Spring Cloud Gateway -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-gateway</artifactId>
        </dependency>
        
        <!-- Nacos Discovery -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>
        
        <!-- Sentinel -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-sentinel</artifactId>
        </dependency>
        
        <!-- Sleuth + Zipkin -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-sleuth</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-sleuth-zipkin</artifactId>
        </dependency>
    </dependencies>
</project>
```

### application.yml

```yaml
server:
  port: 8080

spring:
  application:
    name: mall-gateway
    
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        
    gateway:
      routes:
        # 用户服务
        - id: user-service
          uri: lb://mall-user-service
          predicates:
            - Path=/api/user/**
          filters:
            - StripPrefix=1
            
        # 商品服务
        - id: product-service
          uri: lb://mall-product-service
          predicates:
            - Path=/api/product/**
          filters:
            - StripPrefix=1
            
        # 订单服务
        - id: order-service
          uri: lb://mall-order-service
          predicates:
            - Path=/api/order/**
          filters:
            - StripPrefix=1
            
        # 支付服务
        - id: pay-service
          uri: lb://mall-pay-service
          predicates:
            - Path=/api/pay/**
          filters:
            - StripPrefix=1
      
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "*"
            allowedMethods: "*"
            allowedHeaders: "*"

  zipkin:
    base-url: http://localhost:9411
  sleuth:
    sampler:
      probability: 1.0

management:
  endpoints:
    web:
      exposure:
        include: health,info
```

---

## 👤 第四步: 创建用户服务

### mall-user-service/pom.xml

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.mall</groupId>
        <artifactId>mall-service</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    
    <artifactId>mall-user-service</artifactId>
    
    <dependencies>
        <!-- Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        
        <!-- Nacos -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
        </dependency>
        
        <!-- MyBatis Plus -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-boot-starter</artifactId>
            <version>${mybatis-plus.version}</version>
        </dependency>
        
        <!-- MySQL -->
        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
        </dependency>
        
        <!-- Redis -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        
        <!-- OpenFeign -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-openfeign</artifactId>
        </dependency>
        
        <!-- Sentinel -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-sentinel</artifactId>
        </dependency>
    </dependencies>
</project>
```

### User实体类

```java
package com.mall.user.entity;

import com.baomidou.mybatisplus.annotation.*;
import lombok.Data;
import java.io.Serializable;
import java.time.LocalDateTime;

@Data
@TableName("sys_user")
public class User implements Serializable {
    
    @TableId(type = IdType.AUTO)
    private Long id;
    
    private String username;
    
    private String password;
    
    private String email;
    
    private String phone;
    
    private String nickname;
    
    private String avatar;
    
    private Integer status;
    
    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createTime;
    
    @TableField(fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updateTime;
}
```

### UserController

```java
package com.mall.user.controller;

import com.mall.common.core.result.Result;
import com.mall.user.entity.User;
import com.mall.user.service.UserService;
import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

@Slf4j
@RestController
@RequestMapping("/user")
public class UserController {
    
    @Autowired
    private UserService userService;
    
    @GetMapping("/{id}")
    public Result<User> getUserById(@PathVariable Long id) {
        log.info("查询用户, id: {}", id);
        User user = userService.getById(id);
        return Result.success(user);
    }
    
    @PostMapping
    public Result<Long> createUser(@RequestBody User user) {
        log.info("创建用户: {}", user.getUsername());
        userService.save(user);
        return Result.success(user.getId());
    }
    
    @PutMapping
    public Result<Boolean> updateUser(@RequestBody User user) {
        log.info("更新用户: {}", user.getId());
        boolean success = userService.updateById(user);
        return Result.success(success);
    }
    
    @DeleteMapping("/{id}")
    public Result<Boolean> deleteUser(@PathVariable Long id) {
        log.info("删除用户, id: {}", id);
        boolean success = userService.removeById(id);
        return Result.success(success);
    }
}
```

---

## 🛍️ 第五步: 创建商品服务

（类似用户服务,省略详细代码,参考用户服务实现）

---

## 📦 第六步: 创建订单服务

### 集成OpenFeign调用其他服务

```java
package com.mall.order.feign;

import com.mall.common.core.result.Result;
import com.mall.order.dto.UserDTO;
import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;

@FeignClient(name = "mall-user-service")
public interface UserFeignClient {
    
    @GetMapping("/user/{id}")
    Result<UserDTO> getUserById(@PathVariable("id") Long id);
}
```

### OrderService集成Feign

```java
package com.mall.order.service;

import com.mall.common.core.result.Result;
import com.mall.order.dto.UserDTO;
import com.mall.order.feign.UserFeignClient;
import com.mall.order.feign.ProductFeignClient;
import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

@Slf4j
@Service
public class OrderService {
    
    @Autowired
    private UserFeignClient userFeignClient;
    
    @Autowired
    private ProductFeignClient productFeignClient;
    
    public void createOrder(Long userId, Long productId, Integer quantity) {
        // 1. 查询用户信息
        Result<UserDTO> userResult = userFeignClient.getUserById(userId);
        if (userResult.getCode() != 200) {
            throw new BusinessException("用户不存在");
        }
        
        // 2. 查询商品信息
        // ... 调用商品服务
        
        // 3. 创建订单
        // ... 业务逻辑
        
        // 4. 发送消息(异步扣减库存)
        // ... 使用Stream发送消息
    }
}
```

---

## 💳 第七步: 创建支付服务

### 集成Stream消息驱动

```java
package com.mall.pay.mq;

import com.mall.pay.dto.OrderMessage;
import lombok.extern.slf4j.Slf4j;
import org.springframework.cloud.stream.annotation.StreamListener;
import org.springframework.messaging.handler.annotation.Payload;
import org.springframework.stereotype.Component;

@Slf4j
@Component
public class OrderMessageConsumer {
    
    @StreamListener("order-input")
    public void handleOrderMessage(@Payload OrderMessage message) {
        log.info("收到订单消息: {}", message.getOrderId());
        
        // 处理支付逻辑
        processPayment(message);
    }
    
    private void processPayment(OrderMessage message) {
        // 支付处理
        log.info("处理支付, orderId: {}", message.getOrderId());
    }
}
```

---

## 🎯 第八步: 集成Sentinel熔断降级

### 配置流控规则

```yaml
spring:
  cloud:
    sentinel:
      datasource:
        flow:
          nacos:
            server-addr: localhost:8848
            data-id: mall-order-service-flow-rules
            group-id: DEFAULT_GROUP
            rule-type: flow
```

### Nacos中配置规则

**Data ID**: `mall-order-service-flow-rules`

```json
[
  {
    "resource": "createOrder",
    "limitApp": "default",
    "grade": 1,
    "count": 100,
    "strategy": 0,
    "controlBehavior": 0
  }
]
```

---

## 🔍 第九步: 集成链路追踪

### 所有服务添加依赖

```xml
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-sleuth</artifactId>
</dependency>
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-sleuth-zipkin</artifactId>
</dependency>
```

### 配置Zipkin

```yaml
spring:
  zipkin:
    base-url: http://localhost:9411
  sleuth:
    sampler:
      probability: 1.0
```

### 查看链路

访问 http://localhost:9411,可以看到完整的调用链:

```
Trace ID: abc123
├── mall-gateway (10ms)
├── mall-order-service (50ms)
│   ├── mall-user-service (20ms)
│   └── mall-product-service (30ms)
└── mall-pay-service (100ms)
```

---

## 🚀 第十步: 部署运行

### 启动顺序

```bash
# 1. 启动基础设施(Docker)
docker-compose up -d

# 2. 启动用户服务
cd mall-user-service
mvn spring-boot:run

# 3. 启动商品服务
cd mall-product-service
mvn spring-boot:run

# 4. 启动订单服务
cd mall-order-service
mvn spring-boot:run

# 5. 启动支付服务
cd mall-pay-service
mvn spring-boot:run

# 6. 启动网关
cd mall-gateway
mvn spring-boot:run
```

### 测试接口

```bash
# 1. 创建用户
curl -X POST http://localhost:8080/api/user/user \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"123456"}'

# 2. 创建商品
curl -X POST http://localhost:8080/api/product/product \
  -H "Content-Type: application/json" \
  -d '{"name":"iPhone 15","price":5999}'

# 3. 创建订单
curl -X POST http://localhost:8080/api/order/order \
  -H "Content-Type: application/json" \
  -d '{"userId":1,"productId":1,"quantity":1}'

# 4. 查询链路
访问 http://localhost:9411 查看Trace ID
```

---

## 📊 项目总结

### 技术亮点

✅ **微服务架构**: 服务拆分、独立部署  
✅ **服务治理**: Nacos注册发现  
✅ **API网关**: 统一入口、路由转发  
✅ **服务调用**: OpenFeign声明式调用  
✅ **负载均衡**: LoadBalancer自动负载  
✅ **熔断降级**: Sentinel流量控制  
✅ **配置中心**: Nacos动态配置  
✅ **消息驱动**: Stream异步通信  
✅ **链路追踪**: Sleuth + Zipkin  

### 学习收获

通过这个项目,你将:
- 掌握Spring Cloud全套技术栈
- 理解微服务架构设计思想
- 具备企业级项目开发能力
- 能够独立搭建微服务平台

---

## 🎉 恭喜完成!

你已经成功构建了一个完整的Spring Cloud微服务电商系统!

继续优化和扩展,让它更加完善! 🚀
