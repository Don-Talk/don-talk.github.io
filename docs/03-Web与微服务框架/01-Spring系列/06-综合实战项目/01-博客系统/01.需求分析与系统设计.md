# 博客系统实战项目 - 需求分析与系统设计

## 项目概述

### 1.1 项目背景

**项目名称**: DonTalk Blog  
**技术栈**: Spring Boot + MyBatis-Plus + Spring Security + JWT + Vue.js  
**项目类型**: 前后端分离的个人博客系统  

**学习目标**:
- ✅ 综合运用Spring全家桶技术
- ✅ 掌握企业级项目开发流程
- ✅ 理解前后端分离架构
- ✅ 学习安全认证和权限控制
- ✅ 掌握RESTful API设计规范
- ✅ 实践Docker容器化部署

TODO: [此处需要添加博客系统架构图]

### 1.2 功能需求

#### 核心功能

| 模块 | 功能点 | 优先级 |
|------|--------|--------|
| 用户管理 | 注册、登录、个人信息修改 | P0 |
| 文章管理 | 发布、编辑、删除、查看文章 | P0 |
| 分类标签 | 文章分类、标签管理 | P0 |
| 评论系统 | 发表评论、回复评论、点赞 | P1 |
| 搜索功能 | 关键词搜索文章 | P1 |
| 统计功能 | 阅读量、点赞数统计 | P2 |
| Markdown | 支持Markdown编辑器 | P1 |
| 图片上传 | 文章配图上传 | P0 |

#### 用户角色

| 角色 | 权限 |
|------|------|
| 游客 | 浏览文章、查看评论 |
| 注册用户 | 发表评论、点赞文章 |
| 作者 | 发布/编辑/删除自己的文章 |
| 管理员 | 管理所有文章、用户、评论 |

---

## 二、技术选型

### 2.1 后端技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| Spring Boot | 2.7.x | 核心框架 |
| Spring Security | 5.7.x | 安全框架 |
| JWT | 0.11.x | Token认证 |
| MyBatis-Plus | 3.5.x | ORM框架 |
| MySQL | 8.0 | 数据库 |
| Redis | 7.0 | 缓存 |
| Lombok | 1.18.x | 简化代码 |
| Swagger | 3.0.x | API文档 |

### 2.2 前端技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| Vue.js | 3.x | 前端框架 |
| Element Plus | 2.x | UI组件库 |
| Axios | 1.x | HTTP客户端 |
| Vue Router | 4.x | 路由管理 |
| Pinia | 2.x | 状态管理 |
| Markdown-it | 13.x | Markdown解析 |

### 2.3 开发工具

| 工具 | 说明 |
|------|------|
| IDEA | Java开发IDE |
| VS Code | 前端开发IDE |
| Postman | API测试 |
| Navicat | 数据库管理 |
| Git | 版本控制 |
| Docker | 容器化部署 |

---

## 三、系统设计

### 3.1 系统架构

```
┌─────────────────────────────────────┐
│          前端层 (Vue.js)             │
│  - 用户界面                          │
│  - 路由管理                          │
│  - 状态管理                          │
└──────────────┬──────────────────────┘
               │ HTTP/HTTPS
┌──────────────▼──────────────────────┐
│         网关层 (Nginx)               │
│  - 反向代理                          │
│  - 负载均衡                          │
│  - 静态资源服务                      │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│       应用层 (Spring Boot)           │
│  ┌────────────────────────────┐     │
│  │   Controller层 (API接口)    │     │
│  └────────────┬───────────────┘     │
│  ┌────────────▼───────────────┐     │
│  │   Service层 (业务逻辑)      │     │
│  └────────────┬───────────────┘     │
│  ┌────────────▼───────────────┐     │
│  │   Mapper层 (数据访问)       │     │
│  └────────────┬───────────────┘     │
└───────────────┼─────────────────────┘
                │
     ┌──────────┴──────────┐
     │                     │
┌────▼─────┐        ┌─────▼────┐
│  MySQL   │        │  Redis   │
│ 数据库   │        │  缓存    │
└──────────┘        └──────────┘
```

### 3.2 分层架构

#### Controller层 (表现层)
- 接收HTTP请求
- 参数校验
- 调用Service层
- 返回统一响应

#### Service层 (业务层)
- 业务逻辑处理
- 事务控制
- 调用Mapper层
- 缓存管理

#### Mapper层 (持久层)
- 数据库CRUD操作
- SQL映射
- 使用MyBatis-Plus

#### Entity层 (实体层)
- 数据库表映射
- JPA注解
- Lombok简化

---

## 四、数据库设计

### 4.1 ER图

TODO: [此处需要添加ER关系图]

### 4.2 数据表设计

#### 用户表 (sys_user)

```sql
CREATE TABLE `sys_user` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `username` VARCHAR(50) NOT NULL COMMENT '用户名',
  `password` VARCHAR(100) NOT NULL COMMENT '密码(BCrypt加密)',
  `nickname` VARCHAR(50) COMMENT '昵称',
  `email` VARCHAR(100) COMMENT '邮箱',
  `avatar` VARCHAR(255) COMMENT '头像URL',
  `role` VARCHAR(20) DEFAULT 'USER' COMMENT '角色: ADMIN/AUTHOR/USER',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 0禁用 1启用',
  `login_time` DATETIME COMMENT '最后登录时间',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`),
  UNIQUE KEY `uk_email` (`email`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

#### 文章表 (blog_article)

```sql
CREATE TABLE `blog_article` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '文章ID',
  `title` VARCHAR(200) NOT NULL COMMENT '标题',
  `summary` VARCHAR(500) COMMENT '摘要',
  `content` LONGTEXT NOT NULL COMMENT '内容(Markdown)',
  `cover_image` VARCHAR(255) COMMENT '封面图URL',
  `author_id` BIGINT NOT NULL COMMENT '作者ID',
  `category_id` BIGINT COMMENT '分类ID',
  `view_count` INT DEFAULT 0 COMMENT '阅读量',
  `like_count` INT DEFAULT 0 COMMENT '点赞数',
  `comment_count` INT DEFAULT 0 COMMENT '评论数',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 0草稿 1发布 2删除',
  `is_top` TINYINT DEFAULT 0 COMMENT '是否置顶: 0否 1是',
  `publish_time` DATETIME COMMENT '发布时间',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_author` (`author_id`),
  KEY `idx_category` (`category_id`),
  KEY `idx_status` (`status`),
  KEY `idx_publish_time` (`publish_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文章表';
```

#### 分类表 (blog_category)

```sql
CREATE TABLE `blog_category` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '分类ID',
  `name` VARCHAR(50) NOT NULL COMMENT '分类名称',
  `description` VARCHAR(200) COMMENT '描述',
  `sort_order` INT DEFAULT 0 COMMENT '排序',
  `article_count` INT DEFAULT 0 COMMENT '文章数量',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_name` (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='分类表';
```

#### 标签表 (blog_tag)

```sql
CREATE TABLE `blog_tag` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '标签ID',
  `name` VARCHAR(50) NOT NULL COMMENT '标签名称',
  `article_count` INT DEFAULT 0 COMMENT '文章数量',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_name` (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='标签表';
```

#### 文章标签关联表 (blog_article_tag)

```sql
CREATE TABLE `blog_article_tag` (
  `article_id` BIGINT NOT NULL COMMENT '文章ID',
  `tag_id` BIGINT NOT NULL COMMENT '标签ID',
  PRIMARY KEY (`article_id`, `tag_id`),
  KEY `idx_tag` (`tag_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文章标签关联表';
```

#### 评论表 (blog_comment)

```sql
CREATE TABLE `blog_comment` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '评论ID',
  `article_id` BIGINT NOT NULL COMMENT '文章ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `parent_id` BIGINT DEFAULT 0 COMMENT '父评论ID(0表示顶级评论)',
  `content` TEXT NOT NULL COMMENT '评论内容',
  `like_count` INT DEFAULT 0 COMMENT '点赞数',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 0隐藏 1显示',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_article` (`article_id`),
  KEY `idx_user` (`user_id`),
  KEY `idx_parent` (`parent_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评论表';
```

### 4.3 索引优化

**查询场景分析**:

1. **首页文章列表**: 按发布时间降序,分页查询
   - 索引: `idx_status_publish_time` (status, publish_time DESC)

2. **作者文章列表**: 查询某作者的所有文章
   - 索引: `idx_author_status` (author_id, status)

3. **分类文章列表**: 查询某分类下的文章
   - 索引: `idx_category_status` (category_id, status)

4. **文章详情**: 根据ID查询
   - 主键索引已足够

5. **搜索功能**: 标题或内容关键词搜索
   - 考虑使用全文索引或Elasticsearch

---

## 五、API设计规范

### 5.1 RESTful规范

#### URL设计

```
GET    /api/articles          # 获取文章列表
GET    /api/articles/{id}     # 获取文章详情
POST   /api/articles          # 创建文章
PUT    /api/articles/{id}     # 更新文章
DELETE /api/articles/{id}     # 删除文章
```

#### 统一响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {
    // 业务数据
  }
}
```

**响应码定义**:

| Code | 说明 |
|------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未登录或Token失效 |
| 403 | 没有权限 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### 5.2 认证机制

#### JWT Token

**请求头**:
```
Authorization: Bearer <token>
```

**Token结构**:
```json
{
  "sub": "username",
  "userId": 1,
  "role": "ADMIN",
  "iat": 1234567890,
  "exp": 1234571490
}
```

### 5.3 分页规范

**请求参数**:
```
GET /api/articles?page=1&size=10&sortBy=publishTime&order=desc
```

**响应格式**:
```json
{
  "code": 200,
  "data": {
    "records": [...],
    "total": 100,
    "size": 10,
    "current": 1,
    "pages": 10
  }
}
```

---

## 六、项目结构

### 6.1 后端目录结构

```
dontalk-blog/
├── src/main/java/com/dontalk/blog/
│   ├── BlogApplication.java          # 启动类
│   ├── common/                        # 通用模块
│   │   ├── result/                    # 统一响应
│   │   │   ├── Result.java
│   │   │   └── ResultCode.java
│   │   ├── exception/                 # 异常处理
│   │   │   ├── BusinessException.java
│   │   │   └── GlobalExceptionHandler.java
│   │   └── constant/                  # 常量定义
│   │       └── Constants.java
│   ├── config/                        # 配置类
│   │   ├── WebConfig.java            # Web配置
│   │   ├── SecurityConfig.java       # 安全配置
│   │   ├── MybatisPlusConfig.java    # MP配置
│   │   └── RedisConfig.java          # Redis配置
│   ├── module/                        # 业务模块
│   │   ├── user/                      # 用户模块
│   │   │   ├── controller/
│   │   │   ├── service/
│   │   │   ├── mapper/
│   │   │   ├── entity/
│   │   │   └── dto/
│   │   ├── article/                   # 文章模块
│   │   ├── category/                  # 分类模块
│   │   ├── tag/                       # 标签模块
│   │   └── comment/                   # 评论模块
│   ├── security/                      # 安全模块
│   │   ├── jwt/                       # JWT工具
│   │   ├── filter/                    # 过滤器
│   │   └── handler/                   # 处理器
│   └── utils/                         # 工具类
│       ├── JwtUtil.java
│       └── RedisUtil.java
├── src/main/resources/
│   ├── application.yml               # 配置文件
│   ├── application-dev.yml           # 开发环境
│   ├── application-prod.yml          # 生产环境
│   └── mapper/                       # XML映射文件
└── pom.xml                           # Maven配置
```

### 6.2 前端目录结构

```
dontalk-blog-web/
├── src/
│   ├── views/                         # 页面组件
│   │   ├── Home.vue                  # 首页
│   │   ├── Article/                  # 文章相关
│   │   ├── User/                     # 用户相关
│   │   └── Admin/                    # 后台管理
│   ├── components/                    # 公共组件
│   ├── router/                        # 路由配置
│   ├── store/                         # 状态管理
│   ├── api/                           # API接口
│   ├── utils/                         # 工具函数
│   └── assets/                        # 静态资源
├── public/
├── package.json
└── vite.config.js
```

---

## 七、开发计划

### 7.1 阶段划分

#### 第一阶段: 基础搭建 (1-2天)
- [ ] 创建Spring Boot项目
- [ ] 配置MySQL、Redis
- [ ] 搭建项目结构
- [ ] 配置MyBatis-Plus
- [ ] 配置Swagger文档

#### 第二阶段: 用户模块 (2-3天)
- [ ] 用户注册接口
- [ ] 用户登录接口(JWT)
- [ ] 用户信息管理
- [ ] Spring Security集成

#### 第三阶段: 文章模块 (3-4天)
- [ ] 文章CRUD接口
- [ ] 文章列表分页
- [ ] 文章搜索功能
- [ ] Markdown支持

#### 第四阶段: 分类标签 (1-2天)
- [ ] 分类管理
- [ ] 标签管理
- [ ] 文章标签关联

#### 第五阶段: 评论系统 (2-3天)
- [ ] 发表评论
- [ ] 回复评论
- [ ] 评论树形结构

#### 第六阶段: 前端开发 (5-7天)
- [ ] Vue项目搭建
- [ ] 页面组件开发
- [ ] API对接
- [ ] 样式美化

#### 第七阶段: 部署上线 (1-2天)
- [ ] Docker镜像构建
- [ ] Nginx配置
- [ ] HTTPS证书
- [ ] 性能优化

---

## 总结

本节完成了博客系统的需求分析和系统设计:

**核心要点**:
1. 明确了功能需求和用户角色
2. 选择了成熟的技术栈(Spring Boot + Vue)
3. 设计了合理的数据库表结构
4. 制定了RESTful API规范
5. 规划了清晰的开发计划

**下一步**: 开始搭建项目环境,创建Spring Boot项目并配置基础框架。

准备好开始编码了吗? 🚀
