# 博客系统 - 分类与标签模块开发

## 一、分类模块

### 1.1 Category实体类

```java
package com.dontalk.blog.module.category.entity;

import com.baomidou.mybatisplus.annotation.TableName;
import com.dontalk.blog.common.entity.BaseEntity;
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import lombok.Data;
import lombok.EqualsAndHashCode;

@Data
@EqualsAndHashCode(callSuper = true)
@TableName("category")
@ApiModel(value = "Category对象", description = "文章分类实体")
public class Category extends BaseEntity {
    
    private static final long serialVersionUID = 1L;
    
    @ApiModelProperty("分类名称")
    private String name;
    
    @ApiModelProperty("分类描述")
    private String description;
    
    @ApiModelProperty("分类图标URL")
    private String icon;
    
    @ApiModelProperty("排序号(数字越小越靠前)")
    private Integer sortOrder;
    
    @ApiModelProperty("文章数量")
    private Integer articleCount;
}
```

**字段说明**:
- `sortOrder`: 控制分类展示顺序
- `articleCount`: 统计该分类下的文章数量

---

### 1.2 CategoryMapper.java

```java
package com.dontalk.blog.module.category.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.dontalk.blog.module.category.entity.Category;
import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Select;

import java.util.List;

@Mapper
public interface CategoryMapper extends BaseMapper<Category> {
    
    /**
     * 查询所有分类(按排序号)
     */
    @Select("SELECT * FROM category ORDER BY sort_order ASC, create_time DESC")
    List<Category> findAllOrderBySortOrder();
    
    /**
     * 增加分类文章数
     */
    void incrementArticleCount(Long id);
    
    /**
     * 减少分类文章数
     */
    void decrementArticleCount(Long id);
}
```

### 1.3 CategoryMapper.xml

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dontalk.blog.module.category.mapper.CategoryMapper">
    
    <!-- 增加分类文章数 -->
    <update id="incrementArticleCount">
        UPDATE category SET article_count = article_count + 1 WHERE id = #{id}
    </update>
    
    <!-- 减少分类文章数 -->
    <update id="decrementArticleCount">
        UPDATE category SET article_count = GREATEST(article_count - 1, 0) WHERE id = #{id}
    </update>
    
</mapper>
```

---

### 1.4 CategoryService.java

```java
package com.dontalk.blog.module.category.service;

import com.dontalk.blog.module.category.dto.CategoryCreateRequest;
import com.dontalk.blog.module.category.dto.CategoryUpdateRequest;
import com.dontalk.blog.module.category.vo.CategoryResponse;

import java.util.List;

public interface CategoryService {
    
    /**
     * 创建分类
     */
    Long createCategory(CategoryCreateRequest request);
    
    /**
     * 更新分类
     */
    void updateCategory(CategoryUpdateRequest request);
    
    /**
     * 删除分类
     */
    void deleteCategory(Long id);
    
    /**
     * 查询所有分类
     */
    List<CategoryResponse> getAllCategories();
    
    /**
     * 根据ID查询分类
     */
    CategoryResponse getCategoryById(Long id);
}
```

### 1.5 CategoryServiceImpl.java

```java
package com.dontalk.blog.module.category.service.impl;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.dontalk.blog.common.exception.BusinessException;
import com.dontalk.blog.module.category.dto.CategoryCreateRequest;
import com.dontalk.blog.module.category.dto.CategoryUpdateRequest;
import com.dontalk.blog.module.category.entity.Category;
import com.dontalk.blog.module.category.mapper.CategoryMapper;
import com.dontalk.blog.module.category.service.CategoryService;
import com.dontalk.blog.module.category.vo.CategoryResponse;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.BeanUtils;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;
import java.util.stream.Collectors;

@Slf4j
@Service
@RequiredArgsConstructor
public class CategoryServiceImpl implements CategoryService {
    
    private final CategoryMapper categoryMapper;
    
    @Override
    @Transactional(rollbackFor = Exception.class)
    public Long createCategory(CategoryCreateRequest request) {
        // 1. 检查分类名是否已存在
        LambdaQueryWrapper<Category> wrapper = new LambdaQueryWrapper<>();
        wrapper.eq(Category::getName, request.getName());
        if (categoryMapper.selectCount(wrapper) > 0) {
            throw new BusinessException("分类名已存在");
        }
        
        // 2. 构建分类实体
        Category category = new Category();
        BeanUtils.copyProperties(request, category);
        category.setArticleCount(0);
        
        // 3. 保存分类
        categoryMapper.insert(category);
        
        log.info("创建分类成功: id={}, name={}", category.getId(), category.getName());
        return category.getId();
    }
    
    @Override
    @Transactional(rollbackFor = Exception.class)
    public void updateCategory(CategoryUpdateRequest request) {
        // 1. 查询分类是否存在
        Category category = categoryMapper.selectById(request.getId());
        if (category == null) {
            throw new BusinessException("分类不存在");
        }
        
        // 2. 检查新名称是否与其他分类重复
        if (!category.getName().equals(request.getName())) {
            LambdaQueryWrapper<Category> wrapper = new LambdaQueryWrapper<>();
            wrapper.eq(Category::getName, request.getName())
                   .ne(Category::getId, request.getId());
            if (categoryMapper.selectCount(wrapper) > 0) {
                throw new BusinessException("分类名已存在");
            }
        }
        
        // 3. 更新分类
        BeanUtils.copyProperties(request, category);
        categoryMapper.updateById(category);
        
        log.info("更新分类成功: id={}", category.getId());
    }
    
    @Override
    @Transactional(rollbackFor = Exception.class)
    public void deleteCategory(Long id) {
        // 1. 查询分类是否存在
        Category category = categoryMapper.selectById(id);
        if (category == null) {
            throw new BusinessException("分类不存在");
        }
        
        // 2. 检查是否有文章使用该分类
        if (category.getArticleCount() > 0) {
            throw new BusinessException("该分类下还有文章，无法删除");
        }
        
        // 3. 删除分类
        categoryMapper.deleteById(id);
        
        log.info("删除分类成功: id={}", id);
    }
    
    @Override
    public List<CategoryResponse> getAllCategories() {
        List<Category> categories = categoryMapper.findAllOrderBySortOrder();
        return categories.stream()
                .map(this::convertToResponse)
                .collect(Collectors.toList());
    }
    
    @Override
    public CategoryResponse getCategoryById(Long id) {
        Category category = categoryMapper.selectById(id);
        if (category == null) {
            throw new BusinessException("分类不存在");
        }
        return convertToResponse(category);
    }
    
    // ==================== 私有方法 ====================
    
    private CategoryResponse convertToResponse(Category category) {
        CategoryResponse response = new CategoryResponse();
        BeanUtils.copyProperties(category, response);
        return response;
    }
}
```

---

### 1.6 CategoryController.java

```java
package com.dontalk.blog.module.category.controller;

import com.dontalk.blog.common.result.Result;
import com.dontalk.blog.module.category.dto.CategoryCreateRequest;
import com.dontalk.blog.module.category.dto.CategoryUpdateRequest;
import com.dontalk.blog.module.category.service.CategoryService;
import com.dontalk.blog.module.category.vo.CategoryResponse;
import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;
import lombok.RequiredArgsConstructor;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/api/categories")
@RequiredArgsConstructor
@Api(tags = "分类管理")
public class CategoryController {
    
    private final CategoryService categoryService;
    
    @PostMapping
    @ApiOperation("创建分类")
    public Result<Long> createCategory(@Validated @RequestBody CategoryCreateRequest request) {
        Long categoryId = categoryService.createCategory(request);
        return Result.success(categoryId);
    }
    
    @PutMapping
    @ApiOperation("更新分类")
    public Result<Void> updateCategory(@Validated @RequestBody CategoryUpdateRequest request) {
        categoryService.updateCategory(request);
        return Result.success();
    }
    
    @DeleteMapping("/{id}")
    @ApiOperation("删除分类")
    public Result<Void> deleteCategory(@PathVariable Long id) {
        categoryService.deleteCategory(id);
        return Result.success();
    }
    
    @GetMapping
    @ApiOperation("查询所有分类")
    public Result<List<CategoryResponse>> getAllCategories() {
        List<CategoryResponse> categories = categoryService.getAllCategories();
        return Result.success(categories);
    }
    
    @GetMapping("/{id}")
    @ApiOperation("根据ID查询分类")
    public Result<CategoryResponse> getCategory(@PathVariable Long id) {
        CategoryResponse category = categoryService.getCategoryById(id);
        return Result.success(category);
    }
}
```

---

## 二、标签模块

### 2.1 Tag实体类

```java
package com.dontalk.blog.module.tag.entity;

import com.baomidou.mybatisplus.annotation.TableName;
import com.dontalk.blog.common.entity.BaseEntity;
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import lombok.Data;
import lombok.EqualsAndHashCode;

@Data
@EqualsAndHashCode(callSuper = true)
@TableName("tag")
@ApiModel(value = "Tag对象", description = "文章标签实体")
public class Tag extends BaseEntity {
    
    private static final long serialVersionUID = 1L;
    
    @ApiModelProperty("标签名称")
    private String name;
    
    @ApiModelProperty("标签颜色(十六进制)")
    private String color;
    
    @ApiModelProperty("使用次数")
    private Integer useCount;
}
```

**字段说明**:
- `color`: 前端展示时给标签上色
- `useCount`: 统计该标签被使用的次数

---

### 2.2 TagMapper.java

```java
package com.dontalk.blog.module.tag.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.dontalk.blog.module.tag.entity.Tag;
import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Select;

import java.util.List;

@Mapper
public interface TagMapper extends BaseMapper<Tag> {
    
    /**
     * 查询热门标签(按使用次数降序)
     */
    @Select("SELECT * FROM tag ORDER BY use_count DESC, create_time DESC LIMIT #{limit}")
    List<Tag> findHotTags(int limit);
    
    /**
     * 增加标签使用次数
     */
    void incrementUseCount(Long id);
    
    /**
     * 减少标签使用次数
     */
    void decrementUseCount(Long id);
}
```

### 2.3 TagMapper.xml

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dontalk.blog.module.tag.mapper.TagMapper">
    
    <!-- 增加标签使用次数 -->
    <update id="incrementUseCount">
        UPDATE tag SET use_count = use_count + 1 WHERE id = #{id}
    </update>
    
    <!-- 减少标签使用次数 -->
    <update id="decrementUseCount">
        UPDATE tag SET use_count = GREATEST(use_count - 1, 0) WHERE id = #{id}
    </update>
    
</mapper>
```

---

### 2.4 TagService.java

```java
package com.dontalk.blog.module.tag.service;

import com.dontalk.blog.module.tag.dto.TagCreateRequest;
import com.dontalk.blog.module.tag.dto.TagUpdateRequest;
import com.dontalk.blog.module.tag.vo.TagResponse;

import java.util.List;

public interface TagService {
    
    /**
     * 创建标签
     */
    Long createTag(TagCreateRequest request);
    
    /**
     * 更新标签
     */
    void updateTag(TagUpdateRequest request);
    
    /**
     * 删除标签
     */
    void deleteTag(Long id);
    
    /**
     * 查询所有标签
     */
    List<TagResponse> getAllTags();
    
    /**
     * 查询热门标签
     */
    List<TagResponse> getHotTags(int limit);
    
    /**
     * 根据ID查询标签
     */
    TagResponse getTagById(Long id);
}
```

### 2.5 TagServiceImpl.java

```java
package com.dontalk.blog.module.tag.service.impl;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.dontalk.blog.common.exception.BusinessException;
import com.dontalk.blog.module.tag.dto.TagCreateRequest;
import com.dontalk.blog.module.tag.dto.TagUpdateRequest;
import com.dontalk.blog.module.tag.entity.Tag;
import com.dontalk.blog.module.tag.mapper.TagMapper;
import com.dontalk.blog.module.tag.service.TagService;
import com.dontalk.blog.module.tag.vo.TagResponse;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.BeanUtils;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;
import java.util.stream.Collectors;

@Slf4j
@Service
@RequiredArgsConstructor
public class TagServiceImpl implements TagService {
    
    private final TagMapper tagMapper;
    
    @Override
    @Transactional(rollbackFor = Exception.class)
    public Long createTag(TagCreateRequest request) {
        // 1. 检查标签名是否已存在
        LambdaQueryWrapper<Tag> wrapper = new LambdaQueryWrapper<>();
        wrapper.eq(Tag::getName, request.getName());
        if (tagMapper.selectCount(wrapper) > 0) {
            throw new BusinessException("标签名已存在");
        }
        
        // 2. 构建标签实体
        Tag tag = new Tag();
        BeanUtils.copyProperties(request, tag);
        tag.setUseCount(0);
        
        // 3. 保存标签
        tagMapper.insert(tag);
        
        log.info("创建标签成功: id={}, name={}", tag.getId(), tag.getName());
        return tag.getId();
    }
    
    @Override
    @Transactional(rollbackFor = Exception.class)
    public void updateTag(TagUpdateRequest request) {
        // 1. 查询标签是否存在
        Tag tag = tagMapper.selectById(request.getId());
        if (tag == null) {
            throw new BusinessException("标签不存在");
        }
        
        // 2. 检查新名称是否与其他标签重复
        if (!tag.getName().equals(request.getName())) {
            LambdaQueryWrapper<Tag> wrapper = new LambdaQueryWrapper<>();
            wrapper.eq(Tag::getName, request.getName())
                   .ne(Tag::getId, request.getId());
            if (tagMapper.selectCount(wrapper) > 0) {
                throw new BusinessException("标签名已存在");
            }
        }
        
        // 3. 更新标签
        BeanUtils.copyProperties(request, tag);
        tagMapper.updateById(tag);
        
        log.info("更新标签成功: id={}", tag.getId());
    }
    
    @Override
    @Transactional(rollbackFor = Exception.class)
    public void deleteTag(Long id) {
        // 1. 查询标签是否存在
        Tag tag = tagMapper.selectById(id);
        if (tag == null) {
            throw new BusinessException("标签不存在");
        }
        
        // 2. 检查标签是否被使用
        if (tag.getUseCount() > 0) {
            throw new BusinessException("该标签还在使用中，无法删除");
        }
        
        // 3. 删除标签
        tagMapper.deleteById(id);
        
        log.info("删除标签成功: id={}", id);
    }
    
    @Override
    public List<TagResponse> getAllTags() {
        List<Tag> tags = tagMapper.selectList(null);
        return tags.stream()
                .map(this::convertToResponse)
                .collect(Collectors.toList());
    }
    
    @Override
    public List<TagResponse> getHotTags(int limit) {
        List<Tag> tags = tagMapper.findHotTags(limit);
        return tags.stream()
                .map(this::convertToResponse)
                .collect(Collectors.toList());
    }
    
    @Override
    public TagResponse getTagById(Long id) {
        Tag tag = tagMapper.selectById(id);
        if (tag == null) {
            throw new BusinessException("标签不存在");
        }
        return convertToResponse(tag);
    }
    
    // ==================== 私有方法 ====================
    
    private TagResponse convertToResponse(Tag tag) {
        TagResponse response = new TagResponse();
        BeanUtils.copyProperties(tag, response);
        return response;
    }
}
```

---

### 2.6 TagController.java

```java
package com.dontalk.blog.module.tag.controller;

import com.dontalk.blog.common.result.Result;
import com.dontalk.blog.module.tag.dto.TagCreateRequest;
import com.dontalk.blog.module.tag.dto.TagUpdateRequest;
import com.dontalk.blog.module.tag.service.TagService;
import com.dontalk.blog.module.tag.vo.TagResponse;
import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;
import lombok.RequiredArgsConstructor;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/api/tags")
@RequiredArgsConstructor
@Api(tags = "标签管理")
public class TagController {
    
    private final TagService tagService;
    
    @PostMapping
    @ApiOperation("创建标签")
    public Result<Long> createTag(@Validated @RequestBody TagCreateRequest request) {
        Long tagId = tagService.createTag(request);
        return Result.success(tagId);
    }
    
    @PutMapping
    @ApiOperation("更新标签")
    public Result<Void> updateTag(@Validated @RequestBody TagUpdateRequest request) {
        tagService.updateTag(request);
        return Result.success();
    }
    
    @DeleteMapping("/{id}")
    @ApiOperation("删除标签")
    public Result<Void> deleteTag(@PathVariable Long id) {
        tagService.deleteTag(id);
        return Result.success();
    }
    
    @GetMapping
    @ApiOperation("查询所有标签")
    public Result<List<TagResponse>> getAllTags() {
        List<TagResponse> tags = tagService.getAllTags();
        return Result.success(tags);
    }
    
    @GetMapping("/hot")
    @ApiOperation("查询热门标签")
    public Result<List<TagResponse>> getHotTags(
            @RequestParam(defaultValue = "10") int limit) {
        List<TagResponse> tags = tagService.getHotTags(limit);
        return Result.success(tags);
    }
    
    @GetMapping("/{id}")
    @ApiOperation("根据ID查询标签")
    public Result<TagResponse> getTag(@PathVariable Long id) {
        TagResponse tag = tagService.getTagById(id);
        return Result.success(tag);
    }
}
```

---

## 三、DTO和VO定义

### 3.1 CategoryCreateRequest.java

```java
package com.dontalk.blog.module.category.dto;

import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import lombok.Data;

import javax.validation.constraints.NotBlank;

@Data
@ApiModel(value = "CategoryCreateRequest", description = "创建分类请求")
public class CategoryCreateRequest {
    
    @NotBlank(message = "分类名称不能为空")
    @ApiModelProperty("分类名称")
    private String name;
    
    @ApiModelProperty("分类描述")
    private String description;
    
    @ApiModelProperty("分类图标URL")
    private String icon;
    
    @ApiModelProperty("排序号")
    private Integer sortOrder = 0;
}
```

### 3.2 CategoryUpdateRequest.java

```java
package com.dontalk.blog.module.category.dto;

import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import lombok.Data;

import javax.validation.constraints.NotBlank;
import javax.validation.constraints.NotNull;

@Data
@ApiModel(value = "CategoryUpdateRequest", description = "更新分类请求")
public class CategoryUpdateRequest {
    
    @NotNull(message = "分类ID不能为空")
    @ApiModelProperty("分类ID")
    private Long id;
    
    @NotBlank(message = "分类名称不能为空")
    @ApiModelProperty("分类名称")
    private String name;
    
    @ApiModelProperty("分类描述")
    private String description;
    
    @ApiModelProperty("分类图标URL")
    private String icon;
    
    @ApiModelProperty("排序号")
    private Integer sortOrder;
}
```

### 3.3 CategoryResponse.java

```java
package com.dontalk.blog.module.category.vo;

import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import lombok.Data;

import java.time.LocalDateTime;

@Data
@ApiModel(value = "CategoryResponse", description = "分类响应")
public class CategoryResponse {
    
    @ApiModelProperty("分类ID")
    private Long id;
    
    @ApiModelProperty("分类名称")
    private String name;
    
    @ApiModelProperty("分类描述")
    private String description;
    
    @ApiModelProperty("分类图标URL")
    private String icon;
    
    @ApiModelProperty("排序号")
    private Integer sortOrder;
    
    @ApiModelProperty("文章数量")
    private Integer articleCount;
    
    @ApiModelProperty("创建时间")
    private LocalDateTime createTime;
}
```

### 3.4 TagCreateRequest.java

```java
package com.dontalk.blog.module.tag.dto;

import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import lombok.Data;

import javax.validation.constraints.NotBlank;

@Data
@ApiModel(value = "TagCreateRequest", description = "创建标签请求")
public class TagCreateRequest {
    
    @NotBlank(message = "标签名称不能为空")
    @ApiModelProperty("标签名称")
    private String name;
    
    @ApiModelProperty("标签颜色(十六进制)")
    private String color = "#409EFF";
}
```

### 3.5 TagUpdateRequest.java

```java
package com.dontalk.blog.module.tag.dto;

import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import lombok.Data;

import javax.validation.constraints.NotBlank;
import javax.validation.constraints.NotNull;

@Data
@ApiModel(value = "TagUpdateRequest", description = "更新标签请求")
public class TagUpdateRequest {
    
    @NotNull(message = "标签ID不能为空")
    @ApiModelProperty("标签ID")
    private Long id;
    
    @NotBlank(message = "标签名称不能为空")
    @ApiModelProperty("标签名称")
    private String name;
    
    @ApiModelProperty("标签颜色(十六进制)")
    private String color;
}
```

### 3.6 TagResponse.java

```java
package com.dontalk.blog.module.tag.vo;

import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import lombok.Data;

import java.time.LocalDateTime;

@Data
@ApiModel(value = "TagResponse", description = "标签响应")
public class TagResponse {
    
    @ApiModelProperty("标签ID")
    private Long id;
    
    @ApiModelProperty("标签名称")
    private String name;
    
    @ApiModelProperty("标签颜色(十六进制)")
    private String color;
    
    @ApiModelProperty("使用次数")
    private Integer useCount;
    
    @ApiModelProperty("创建时间")
    private LocalDateTime createTime;
}
```

---

## 四、数据库表设计补充

### 4.1 创建表的SQL

```sql
-- 分类表
CREATE TABLE `category` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `name` varchar(50) NOT NULL COMMENT '分类名称',
  `description` varchar(200) DEFAULT NULL COMMENT '分类描述',
  `icon` varchar(255) DEFAULT NULL COMMENT '分类图标URL',
  `sort_order` int DEFAULT '0' COMMENT '排序号',
  `article_count` int DEFAULT '0' COMMENT '文章数量',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_name` (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文章分类表';

-- 标签表
CREATE TABLE `tag` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `name` varchar(50) NOT NULL COMMENT '标签名称',
  `color` varchar(20) DEFAULT '#409EFF' COMMENT '标签颜色',
  `use_count` int DEFAULT '0' COMMENT '使用次数',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_name` (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文章标签表';

-- 文章标签关联表
CREATE TABLE `article_tag` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `article_id` bigint NOT NULL COMMENT '文章ID',
  `tag_id` bigint NOT NULL COMMENT '标签ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_article_tag` (`article_id`, `tag_id`),
  KEY `idx_tag_id` (`tag_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文章标签关联表';
```

---

## 五、初始化数据

### 5.1 data.sql

```sql
-- 插入初始分类数据
INSERT INTO `category` (`name`, `description`, `icon`, `sort_order`) VALUES
('技术分享', '编程技术、框架学习', '💻', 1),
('生活随笔', '日常感悟、生活记录', '📝', 2),
('读书笔记', '书籍阅读心得', '📚', 3),
('项目实战', '实际项目开发经验', '🚀', 4);

-- 插入初始标签数据
INSERT INTO `tag` (`name`, `color`) VALUES
('Java', '#F89901'),
('Spring Boot', '#6DB33F'),
('MySQL', '#4479A1'),
('Redis', '#DC382D'),
('Vue.js', '#4FC08D'),
('Docker', '#2496ED'),
('微服务', '#FF6B6B'),
('分布式', '#4ECDC4');
```

---

## 六、Postman接口测试

### 6.1 创建分类

**请求**:
```
POST http://localhost:8080/api/categories
Content-Type: application/json
Authorization: Bearer {token}

{
  "name": "技术分享",
  "description": "编程技术、框架学习",
  "icon": "💻",
  "sortOrder": 1
}
```

**响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": 1
}
```

### 6.2 查询所有分类

**请求**:
```
GET http://localhost:8080/api/categories
```

**响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "技术分享",
      "description": "编程技术、框架学习",
      "icon": "💻",
      "sortOrder": 1,
      "articleCount": 10,
      "createTime": "2024-01-01T10:00:00"
    },
    {
      "id": 2,
      "name": "生活随笔",
      "description": "日常感悟、生活记录",
      "icon": "📝",
      "sortOrder": 2,
      "articleCount": 5,
      "createTime": "2024-01-01T10:00:00"
    }
  ]
}
```

### 6.3 创建标签

**请求**:
```
POST http://localhost:8080/api/tags
Content-Type: application/json
Authorization: Bearer {token}

{
  "name": "Java",
  "color": "#F89901"
}
```

**响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": 1
}
```

### 6.4 查询热门标签

**请求**:
```
GET http://localhost:8080/api/tags/hot?limit=5
```

**响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "Java",
      "color": "#F89901",
      "useCount": 50,
      "createTime": "2024-01-01T10:00:00"
    },
    {
      "id": 2,
      "name": "Spring Boot",
      "color": "#6DB33F",
      "useCount": 30,
      "createTime": "2024-01-01T10:00:00"
    }
  ]
}
```

---

## 七、常见问题

### Q1: 分类和标签有什么区别?

**A**: 
- **分类**:  hierarchical(层级)、互斥性(一篇文章只能有一个分类)、粗粒度
- **标签**: flat(扁平)、灵活性(一篇文章可以有多个标签)、细粒度

### Q2: 为什么需要article_tag关联表?

**A**: 多对多关系必须通过中间表实现:
- 一篇文章可以有多个标签
- 一个标签可以被多篇文章使用
- 关联表便于统计和管理

### Q3: 如何优化标签查询性能?

**A**: 
1. **索引**: 在`tag.name`和`tag.use_count`上建立索引
2. **缓存**: Redis缓存热门标签
3. **定时统计**: 每天凌晨重新计算use_count

---

## 八、总结

本节完成了分类和标签模块的完整开发:
- ✅ 分类CRUD(唯一性校验、排序)
- ✅ 标签CRUD(热门标签查询)
- ✅ 多对多关联表设计
- ✅ 初始化数据脚本
- ✅ Postman接口测试

**下一节**: 评论系统模块开发
