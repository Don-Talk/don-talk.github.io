# Spring AOP面向切面编程完全指南

## 学习目标

- [ ] 理解AOP的核心概念和术语
- [ ] 掌握AspectJ注解开发
- [ ] 能够实现日志、事务、权限控制等常见场景
- [ ] 理解AOP的底层原理(JDK动态代理 vs CGLIB)
- [ ] 掌握声明式事务管理

## 一、AOP核心概念

### 1.1 什么是AOP

**AOP (Aspect-Oriented Programming) 面向切面编程**:

将通用功能从业务逻辑中抽离出来,以"横切"的方式织入到目标方法中,实现代码解耦。

**通俗理解**:
- OOP是纵向继承,AOP是横向抽取
- 把重复的代码(日志、事务、权限)提取出来,统一处理
- 不修改原有代码,就能增强功能

TODO: [此处需要添加AOP横切示意图]

### 1.2 AOP核心术语

| 术语 | 英文 | 说明 | 示例 |
|------|------|------|------|
| 连接点 | JoinPoint | 可以被增强的方法 | UserService的所有方法 |
| 切入点 | Pointcut | 实际被增强的方法 | UserService.saveUser() |
| 通知/增强 | Advice | 增强的逻辑 | 日志记录、事务管理 |
| 切面 | Aspect | 切入点 + 通知 | 日志切面、事务切面 |
| 目标对象 | Target | 被代理的对象 | UserServiceImpl |
| 代理对象 | Proxy | 增强后的对象 | JDK/CGLIB代理 |
| 织入 | Weaving | 将增强应用到目标的过程 | Spring容器启动时 |

### 1.3 AOP应用场景

**典型应用**:
1. **日志记录**: 记录方法执行时间、参数、返回值
2. **事务管理**: 统一控制事务的开启、提交、回滚
3. **权限控制**: 检查用户是否有权限执行某操作
4. **性能监控**: 统计方法执行耗时
5. **异常处理**: 统一捕获和处理异常
6. **缓存管理**: 方法结果缓存

---

## 二、AOP快速入门

### 2.1 环境搭建

#### 第一步: 导入依赖

```xml
<dependencies>
    <!--Spring Context-->
    <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-context</artifactId>
        <version>5.3.20</version>
    </dependency>
    
    <!--Spring AOP-->
    <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-aop</artifactId>
        <version>5.3.20</version>
    </dependency>
    
    <!--AspectJ-->
    <dependency>
        <groupId>org.aspectj</groupId>
        <artifactId>aspectjweaver</artifactId>
        <version>1.9.7</version>
    </dependency>
</dependencies>
```

#### 第二步: 启用AOP

**XML方式**:

```xml
<!--开启AspectJ自动代理-->
<aop:aspectj-autoproxy/>
```

**注解方式**:

```java
@Configuration
@EnableAspectJAutoProxy
public class AopConfig {
    // ...
}
```

### 2.2 创建目标类

```java
package com.example.service;

import org.springframework.stereotype.Service;

@Service
public class UserService {
    
    public void saveUser(String username) {
        System.out.println("保存用户: " + username);
        // 模拟业务逻辑
        try {
            Thread.sleep(100);
        } catch (InterruptedException e) {
            e.printStackTrace();
        }
    }
    
    public void deleteUser(Long id) {
        System.out.println("删除用户: " + id);
    }
    
    public String getUser(Long id) {
        System.out.println("查询用户: " + id);
        return "用户" + id;
    }
}
```

### 2.3 创建切面类

```java
package com.example.aspect;

import org.aspectj.lang.JoinPoint;
import org.aspectj.lang.ProceedingJoinPoint;
import org.aspectj.lang.annotation.*;
import org.springframework.stereotype.Component;

@Aspect  // 标识为切面
@Component
public class LogAspect {
    
    /**
     * 前置通知: 目标方法执行前执行
     */
    @Before("execution(* com.example.service.UserService.*(..))")
    public void beforeAdvice(JoinPoint joinPoint) {
        System.out.println("【前置通知】方法: " + joinPoint.getSignature().getName());
        System.out.println("【前置通知】参数: " + java.util.Arrays.toString(joinPoint.getArgs()));
    }
    
    /**
     * 后置通知: 目标方法执行后执行(无论是否异常)
     */
    @After("execution(* com.example.service.UserService.*(..))")
    public void afterAdvice(JoinPoint joinPoint) {
        System.out.println("【后置通知】方法: " + joinPoint.getSignature().getName());
    }
    
    /**
     * 返回通知: 目标方法正常返回后执行
     */
    @AfterReturning(pointcut = "execution(* com.example.service.UserService.*(..))", 
                    returning = "result")
    public void afterReturningAdvice(JoinPoint joinPoint, Object result) {
        System.out.println("【返回通知】方法: " + joinPoint.getSignature().getName());
        System.out.println("【返回通知】返回值: " + result);
    }
    
    /**
     * 异常通知: 目标方法抛出异常后执行
     */
    @AfterThrowing(pointcut = "execution(* com.example.service.UserService.*(..))", 
                   throwing = "exception")
    public void afterThrowingAdvice(JoinPoint joinPoint, Exception exception) {
        System.out.println("【异常通知】方法: " + joinPoint.getSignature().getName());
        System.out.println("【异常通知】异常: " + exception.getMessage());
    }
    
    /**
     * 环绕通知: 包围目标方法,可以控制是否执行目标方法
     */
    @Around("execution(* com.example.service.UserService.*(..))")
    public Object aroundAdvice(ProceedingJoinPoint joinPoint) throws Throwable {
        System.out.println("【环绕通知-前】方法: " + joinPoint.getSignature().getName());
        
        long startTime = System.currentTimeMillis();
        
        // 执行目标方法
        Object result = joinPoint.proceed();
        
        long endTime = System.currentTimeMillis();
        System.out.println("【环绕通知-后】耗时: " + (endTime - startTime) + "ms");
        
        return result;
    }
}
```

### 2.4 测试

```java
package com.example;

import com.example.service.UserService;
import org.springframework.context.ApplicationContext;
import org.springframework.context.annotation.AnnotationConfigApplicationContext;

public class AopTest {
    
    public static void main(String[] args) {
        ApplicationContext ctx = new AnnotationConfigApplicationContext(AopConfig.class);
        
        UserService userService = ctx.getBean(UserService.class);
        
        System.out.println("=== 测试saveUser ===");
        userService.saveUser("张三");
        
        System.out.println("\n=== 测试getUser ===");
        String user = userService.getUser(1L);
        System.out.println("获取到: " + user);
    }
}
```

**输出**:

```
=== 测试saveUser ===
【环绕通知-前】方法: saveUser
【前置通知】方法: saveUser
【前置通知】参数: [张三]
保存用户: 张三
【后置通知】方法: saveUser
【返回通知】方法: saveUser
【返回通知】返回值: null
【环绕通知-后】耗时: 102ms

=== 测试getUser ===
【环绕通知-前】方法: getUser
【前置通知】方法: getUser
【前置通知】参数: [1]
查询用户: 1
【后置通知】方法: getUser
【返回通知】方法: getUser
【返回通知】返回值: 用户1
【环绕通知-后】耗时: 0ms
```

---

## 三、切入点表达式

### 3.1 execution表达式语法

```
execution(修饰符? 返回值类型 方法名(参数) 抛出异常?)
```

**示例**:

```java
// 匹配所有public方法
execution(public * *(..))

// 匹配UserService的所有方法
execution(* com.example.service.UserService.*(..))

// 匹配指定包下所有类的所有方法
execution(* com.example.service..*(..))

// 匹配返回值是String的方法
execution(String *(..))

// 匹配有一个Long参数的方法
execution(* *(Long))

// 匹配有任意数量参数的方法
execution(* *(..))

// 组合条件
execution(* com.example.service.UserService.save*(..))  // save开头的方法
```

### 3.2 其他切入点表达式

| 表达式 | 说明 | 示例 |
|--------|------|------|
| execution | 匹配方法执行 | `execution(* com.example..*.*(..))` |
| within | 匹配指定类型内的方法 | `within(com.example.service.*)` |
| this | 匹配代理对象是指定类型 | `this(com.example.service.UserService)` |
| target | 匹配目标对象是指定类型 | `target(com.example.service.UserService)` |
| args | 匹配参数类型 | `args(java.lang.String)` |
| @annotation | 匹配带有指定注解的方法 | `@annotation(org.springframework.transaction.annotation.Transactional)` |

### 3.3 使用@Pointcut定义切入点

**避免重复书写切入点表达式**:

```java
@Aspect
@Component
public class LogAspect {
    
    /**
     * 定义切入点
     */
    @Pointcut("execution(* com.example.service.UserService.*(..))")
    public void userServicePointcut() {}
    
    @Pointcut("@annotation(org.springframework.transaction.annotation.Transactional)")
    public void transactionalPointcut() {}
    
    /**
     * 引用切入点
     */
    @Before("userServicePointcut()")
    public void beforeAdvice(JoinPoint joinPoint) {
        // ...
    }
    
    @Around("transactionalPointcut()")
    public Object aroundAdvice(ProceedingJoinPoint joinPoint) throws Throwable {
        // ...
    }
}
```

---

## 四、实战案例

### 4.1 日志记录切面

```java
@Aspect
@Component
@Slf4j
public class LogAspect {
    
    @Pointcut("@annotation(com.example.annotation.OperationLog)")
    public void logPointcut() {}
    
    @Around("logPointcut()")
    public Object around(ProceedingJoinPoint joinPoint) throws Throwable {
        MethodSignature signature = (MethodSignature) joinPoint.getSignature();
        Method method = signature.getMethod();
        OperationLog operationLog = method.getAnnotation(OperationLog.class);
        
        String className = joinPoint.getTarget().getClass().getName();
        String methodName = method.getName();
        Object[] args = joinPoint.getArgs();
        
        log.info("【操作日志】类: {}, 方法: {}, 描述: {}", 
                 className, methodName, operationLog.value());
        log.info("【操作日志】参数: {}", JSON.toJSONString(args));
        
        long startTime = System.currentTimeMillis();
        
        try {
            Object result = joinPoint.proceed();
            
            long endTime = System.currentTimeMillis();
            log.info("【操作日志】返回值: {}, 耗时: {}ms", 
                     JSON.toJSONString(result), (endTime - startTime));
            
            return result;
        } catch (Exception e) {
            log.error("【操作日志】异常: {}", e.getMessage(), e);
            throw e;
        }
    }
}
```

**自定义注解**:

```java
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface OperationLog {
    String value() default "";
}
```

**使用**:

```java
@Service
public class OrderService {
    
    @OperationLog("创建订单")
    public Long createOrder(OrderDTO orderDTO) {
        // ...
        return orderId;
    }
}
```

### 4.2 权限控制切面

```java
@Aspect
@Component
public class PermissionAspect {
    
    @Autowired
    private UserService userService;
    
    @Pointcut("@annotation(com.example.annotation.RequirePermission)")
    public void permissionPointcut() {}
    
    @Before("permissionPointcut() && @annotation(requirePermission)")
    public void checkPermission(RequirePermission requirePermission) {
        // 获取当前登录用户
        String currentUser = SecurityContextHolder.getContext().getAuthentication().getName();
        
        // 检查权限
        boolean hasPermission = userService.hasPermission(currentUser, requirePermission.value());
        
        if (!hasPermission) {
            throw new AccessDeniedException("没有权限执行此操作");
        }
    }
}
```

**使用**:

```java
@RestController
@RequestMapping("/admin")
public class AdminController {
    
    @RequirePermission("admin:user:delete")
    @DeleteMapping("/user/{id}")
    public Result<Void> deleteUser(@PathVariable Long id) {
        userService.deleteUser(id);
        return Result.success();
    }
}
```

### 4.3 性能监控切面

```java
@Aspect
@Component
@Slf4j
public class PerformanceAspect {
    
    @Pointcut("execution(* com.example.service..*(..))")
    public void servicePointcut() {}
    
    @Around("servicePointcut()")
    public Object monitorPerformance(ProceedingJoinPoint joinPoint) throws Throwable {
        long startTime = System.currentTimeMillis();
        
        try {
            return joinPoint.proceed();
        } finally {
            long endTime = System.currentTimeMillis();
            long cost = endTime - startTime;
            
            // 超过1秒的方法记录警告
            if (cost > 1000) {
                log.warn("【性能警告】{}.{} 耗时: {}ms", 
                         joinPoint.getTarget().getClass().getSimpleName(),
                         joinPoint.getSignature().getName(),
                         cost);
            }
        }
    }
}
```

---

## 五、AOP底层原理

### 5.1 两种代理方式

| 特性 | JDK动态代理 | CGLIB代理 |
|------|------------|-----------|
| 要求 | 目标类必须实现接口 | 无要求 |
| 原理 | 反射机制 | 字节码生成 |
| 性能 | 略慢 | 略快(Spring 5后优化) |
| 限制 | 只能代理接口方法 | 不能代理final方法 |

### 5.2 Spring如何选择代理

**默认规则**:
1. 如果目标类实现了接口 → 使用JDK动态代理
2. 如果目标类没有实现接口 → 使用CGLIB代理

**强制使用CGLIB**:

```yaml
spring:
  aop:
    proxy-target-class: true
```

或

```java
@EnableAspectJAutoProxy(proxyTargetClass = true)
```

### 5.3 JDK动态代理示例

```java
public class JdkProxyFactory {
    
    public static <T> T createProxy(T target, Class<T> interfaceClass) {
        return (T) Proxy.newProxyInstance(
            interfaceClass.getClassLoader(),
            new Class<?>[]{interfaceClass},
            (proxy, method, args) -> {
                System.out.println("【代理】方法执行前");
                
                Object result = method.invoke(target, args);
                
                System.out.println("【代理】方法执行后");
                
                return result;
            }
        );
    }
}
```

---

## 总结

本节全面讲解了Spring AOP的核心知识和实战应用:

**核心要点**:
1. AOP将横切关注点从业务逻辑中分离
2. 五种通知类型: Before、After、AfterReturning、AfterThrowing、Around
3. 切入点表达式灵活匹配目标方法
4. JDK动态代理和CGLIB是两种底层实现
5. AOP广泛应用于日志、事务、权限控制等场景

**最佳实践**:
- 优先使用@Around通知,功能最强大
- 使用@Pointcut避免重复书写表达式
- 合理选择代理方式(有接口用JDK,无接口用CGLIB)
- 注意AOP不能拦截内部调用(self-invocation)
- 性能敏感场景谨慎使用AOP

**下一步**: 学习声明式事务管理,掌握@Transactional的使用。
