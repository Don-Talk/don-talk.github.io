# 11Spring Boot集成最佳实践

**学习目标：**
> * 掌握 starter 自动配置原理
> * 能够配置多数据源
> * 实现动态数据源切换
> * 理解事务管理集成
> * 掌握生产环境配置优化

---

## 1. Starter 自动配置原理

### 1.1 依赖引入

```xml
<dependency>
    <groupId>com.baomidou</groupId>
    <artifactId>mybatis-plus-boot-starter</artifactId>
    <version>3.5.3.1</version>
</dependency>
```

### 1.2 自动配置类

`MybatisPlusAutoConfiguration` 负责自动配置：
* SqlSessionFactory
* SqlSessionTemplate
* MapperScannerConfigurer

### 1.3 配置优先级

```yaml
# application.yml - 低优先级
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# application-dev.yml - 中优先级
# application-prod.yml - 高优先级
```

---

## 2. 多数据源配置

### 2.1 添加依赖

```xml
<dependency>
    <groupId>com.baomidou</groupId>
    <artifactId>dynamic-datasource-spring-boot-starter</artifactId>
    <version>3.5.2</version>
</dependency>
```

### 2.2 配置文件

```yaml
spring:
  datasource:
    dynamic:
      primary: master          # 默认数据源
      strict: false            # 严格模式
      datasource:
        master:                # 主库
          url: jdbc:mysql://localhost:3306/db_master
          username: root
          password: 123456
          driver-class-name: com.mysql.cj.jdbc.Driver
        slave_1:               # 从库1
          url: jdbc:mysql://localhost:3306/db_slave1
          username: root
          password: 123456
          driver-class-name: com.mysql.cj.jdbc.Driver
        slave_2:               # 从库2
          url: jdbc:mysql://localhost:3306/db_slave2
          username: root
          password: 123456
          driver-class-name: com.mysql.cj.jdbc.Driver
```

### 2.3 使用注解切换

```java
@Service
public class UserService {
    
    @Autowired
    private UserMapper userMapper;
    
    /**
     * 使用主库（写操作）
     */
    @DS("master")
    public void saveUser(User user) {
        userMapper.insert(user);
    }
    
    /**
     * 使用从库（读操作）
     */
    @DS("slave_1")
    public List<User> listUsers() {
        return userMapper.selectList(null);
    }
    
    /**
     * 自动负载均衡
     */
    @DS("slave")  // slave 表示所有从库
    public User getUserById(Long id) {
        return userMapper.selectById(id);
    }
}
```

### 2.4 AOP 自动切换

```java
@Aspect
@Component
public class DataSourceAspect {
    
    @Before("@annotation(readOnly)")
    public void setReadDataSource(ReadOnly readOnly) {
        // 读操作自动切换到从库
        DynamicDataSourceContextHolder.push("slave");
    }
    
    @After("@annotation(readOnly)")
    public void restoreDataSource(ReadOnly readOnly) {
        DynamicDataSourceContextHolder.poll();
    }
}

// 自定义注解
@Target({ElementType.METHOD})
@Retention(RetentionPolicy.RUNTIME)
public @interface ReadOnly {
}
```

---

## 3. 事务管理

### 3.1 声明式事务

```java
@Service
@Transactional
public class OrderService {
    
    @Autowired
    private OrderMapper orderMapper;
    
    @Autowired
    private OrderItemMapper itemMapper;
    
    public void createOrder(Order order, List<OrderItem> items) {
        // 插入订单
        orderMapper.insert(order);
        
        // 插入订单项
        for (OrderItem item : items) {
            item.setOrderId(order.getId());
            itemMapper.insert(item);
        }
        
        // 如果抛出异常，自动回滚
    }
}
```

### 3.2 编程式事务

```java
@Service
public class ProductService {
    
    @Autowired
    private TransactionTemplate transactionTemplate;
    
    public void updateStock(Long productId, int quantity) {
        transactionTemplate.execute(status -> {
            try {
                Product product = productMapper.selectById(productId);
                product.setStock(product.getStock() - quantity);
                productMapper.updateById(product);
                return true;
            } catch (Exception e) {
                status.setRollbackOnly();  // 手动回滚
                throw e;
            }
        });
    }
}
```

### 3.3 事务传播行为

```java
@Service
public class BusinessService {
    
    @Autowired
    private UserService userService;
    
    @Autowired
    private OrderService orderService;
    
    /**
     * REQUIRED（默认）：如果存在事务则加入，否则新建
     */
    @Transactional(propagation = Propagation.REQUIRED)
    public void method1() {
        userService.saveUser(new User());
        orderService.createOrder(new Order(), new ArrayList<>());
    }
    
    /**
     * REQUIRES_NEW：总是新建事务
     */
    @Transactional(propagation = Propagation.REQUIRES_NEW)
    public void method2() {
        // 独立事务，不受外部事务影响
    }
    
    /**
     * NESTED：嵌套事务
     */
    @Transactional(propagation = Propagation.NESTED)
    public void method3() {
        // 可以单独回滚
    }
}
```

---

## 4. 生产环境配置

### 4.1 application-prod.yml

```yaml
server:
  port: 8080

spring:
  datasource:
    hikari:
      minimum-idle: 10
      maximum-pool-size: 50
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      pool-name: MyHikariCP

mybatis-plus:
  # 实体类别名
  type-aliases-package: com.example.entity
  
  # Mapper XML 位置
  mapper-locations: classpath:mapper/*.xml
  
  configuration:
    # 开启驼峰映射
    map-underscore-to-camel-case: true
    
    # 关闭 SQL 日志（生产环境）
    # log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
    
    # 缓存配置
    cache-enabled: true
    
  global-config:
    db-config:
      # 主键策略
      id-type: assign_id
      
      # 逻辑删除
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
      
    # 关闭 banner
    banner: false
```

### 4.2 性能优化

```java
@Configuration
public class MybatisPlusConfig {
    
    @Bean
    public MybatisPlusInterceptor mybatisPlusInterceptor() {
        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();
        
        // 分页插件
        interceptor.addInnerInterceptor(
            new PaginationInnerInterceptor(DbType.MYSQL));
        
        // 乐观锁插件
        interceptor.addInnerInterceptor(
            new OptimisticLockerInnerInterceptor());
        
        // 注意：生产环境不启用性能分析插件
        
        return interceptor;
    }
    
    /**
     * 分页插件配置
     */
    @Bean
    public ConfigurationCustomizer configurationCustomizer() {
        return configuration -> {
            // 启用延迟加载
            configuration.setLazyLoadingEnabled(true);
            configuration.setAggressiveLazyLoading(false);
        };
    }
}
```

---

## 5. 监控与诊断

### 5.1 Druid 监控

```xml
<dependency>
    <groupId>com.alibaba</groupId>
    <artifactId>druid-spring-boot-starter</artifactId>
    <version>1.2.18</version>
</dependency>
```

```yaml
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      stat-view-servlet:
        enabled: true
        url-pattern: /druid/*
        login-username: admin
        login-password: admin
```

访问 `http://localhost:8080/druid/` 查看监控面板。

### 5.2 MP 统计信息

```java
@RestController
@RequestMapping("/admin/mp-stats")
public class MpStatsController {
    
    @Autowired
    private SqlSessionFactory sqlSessionFactory;
    
    @GetMapping
    public Map<String, Object> getStats() {
        Statistics statistics = sqlSessionFactory.getConfiguration().getStatistics();
        
        Map<String, Object> stats = new HashMap<>();
        stats.put("connectCount", statistics.getConnectCount());
        stats.put("statementCount", statistics.getStatementCount());
        stats.put("successStatementCount", statistics.getSuccessStatementCount());
        stats.put("errorStatementCount", statistics.getErrorStatementCount());
        
        return stats;
    }
}
```

---

## 6. 常见问题

### 6.1 循环依赖

**问题**：Service 之间相互注入导致循环依赖

**解决**：
```java
// 方式一：使用 @Lazy
@Autowired
@Lazy
private OtherService otherService;

// 方式二：重构代码，提取公共逻辑
```

### 6.2 事务失效

**问题**：@Transactional 不生效

**原因**：
* 方法不是 public
* 同类内部调用
* 异常被捕获

**解决**：
```java
// ✅ 正确
@Transactional
public void method() {
    // ...
}

// ❌ 错误：同类内部调用
public void outerMethod() {
    this.innerMethod();  // 事务失效
}

@Transactional
public void innerMethod() {
    // ...
}
```

### 6.3 内存泄漏

**问题**：长时间运行后内存溢出

**原因**：SqlSession 未关闭

**解决**：使用 Spring 管理，自动关闭

---

## 7. 总结

### 7.1 核心知识点

✅ **Starter 原理**：自动配置  
✅ **多数据源**：dynamic-datasource  
✅ **事务管理**：@Transactional、Propagation  
✅ **生产配置**：连接池、日志、缓存  
✅ **监控诊断**：Druid、MP 统计  

### 7.2 最佳实践

📌 读写分离使用多数据源  
📌 合理设置事务传播行为  
📌 生产环境关闭 SQL 日志  
📌 使用连接池监控  
📌 定期清理慢查询  

---

## 8. 下一步学习

📖 [12.性能调优与常见问题](../12.性能调优与常见问题.md)  
📖 [13.实战项目：完整应用开发](../13.实战项目完整应用开发.md)

---

**参考资料：**
* MyBatis-Plus 官方文档：https://baomidou.com/
* Dynamic Datasource：https://github.com/baomidou/dynamic-datasource-spring-boot-starter
