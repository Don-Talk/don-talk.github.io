# 05自动填充与字段处理器

**学习目标：**
> * 理解自动填充的作用和场景
> * 掌握 @TableField 注解的使用
> * 能够实现 MetaObjectHandler
> * 完成创建时间、更新时间的自动填充
> * 了解自定义字段处理器

---

## 1. 自动填充概述

### 1.1 什么是自动填充？

自动填充是指在插入或更新数据时，MP 自动为某些字段设置值，无需手动赋值。

**常见场景**：
* 创建时间（create_time）
* 更新时间（update_time）
* 创建人（create_by）
* 更新人（update_by）
* 版本号（version）

### 1.2 FieldFill 枚举

```java
public enum FieldFill {
    DEFAULT,        // 默认不处理
    INSERT,         // 插入时填充
    UPDATE,         // 更新时填充
    INSERT_UPDATE   // 插入和更新时填充
}
```

---

## 2. @TableField 注解详解

### 2.1 基本用法

```java
@Data
@TableName("user")
public class User {
    
    @TableId(type = IdType.AUTO)
    private Long id;
    
    @TableField("username")  // 指定数据库字段名
    private String username;
    
    @TableField(fill = FieldFill.INSERT)  // 插入时填充
    private LocalDateTime createTime;
    
    @TableField(fill = FieldFill.INSERT_UPDATE)  // 插入和更新时填充
    private LocalDateTime updateTime;
    
    @TableField(exist = false)  // 非数据库字段
    private String extraInfo;
}
```

### 2.2 常用属性

| 属性 | 说明 | 示例 |
|------|------|------|
| `value` | 数据库字段名 | `@TableField("user_name")` |
| `fill` | 自动填充策略 | `@TableField(fill = FieldFill.INSERT)` |
| `exist` | 是否为数据库字段 | `@TableField(exist = false)` |
| `select` | 是否参与查询 | `@TableField(select = false)` |
| `condition` | 条件判断 | `@TableField(condition = SqlCondition.LIKE)` |

---

## 3. MetaObjectHandler 实现

### 3.1 创建处理器

```java
package com.example.handler;

import com.baomidou.mybatisplus.core.handlers.MetaObjectHandler;
import lombok.extern.slf4j.Slf4j;
import org.apache.ibatis.reflection.MetaObject;
import org.springframework.stereotype.Component;

import java.time.LocalDateTime;

/**
 * 自动填充处理器
 */
@Slf4j
@Component
public class MyMetaObjectHandler implements MetaObjectHandler {
    
    /**
     * 插入时的填充策略
     */
    @Override
    public void insertFill(MetaObject metaObject) {
        log.info("开始插入填充...");
        
        // 方式一：严格填充（字段不存在会报错）
        this.strictInsertFill(metaObject, "createTime", 
                             LocalDateTime.class, LocalDateTime.now());
        
        // 方式二：普通填充（推荐）
        this.fillStrategy(metaObject, "createTime", LocalDateTime.now());
        this.fillStrategy(metaObject, "updateTime", LocalDateTime.now());
    }
    
    /**
     * 更新时的填充策略
     */
    @Override
    public void updateFill(MetaObject metaObject) {
        log.info("开始更新填充...");
        
        this.fillStrategy(metaObject, "updateTime", LocalDateTime.now());
    }
}
```

### 3.2 填充方法说明

| 方法 | 说明 |
|------|------|
| `strictInsertFill` | 严格插入填充（字段必须存在） |
| `strictUpdateFill` | 严格更新填充 |
| `fillStrategy` | 普通填充策略（推荐） |

---

## 4. 实战：时间自动填充

### 4.1 实体类

```java
@Data
@TableName("user")
public class User {
    
    @TableId(type = IdType.AUTO)
    private Long id;
    
    private String username;
    
    private String email;
    
    private Integer age;
    
    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createTime;
    
    @TableField(fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updateTime;
}
```

### 4.2 测试插入

```java
@Test
public void testInsert() {
    User user = new User();
    user.setUsername("测试用户");
    user.setEmail("test@example.com");
    user.setAge(25);
    
    // 无需手动设置 createTime 和 updateTime
    userMapper.insert(user);
    
    System.out.println("创建时间：" + user.getCreateTime());
    System.out.println("更新时间：" + user.getUpdateTime());
}
```

**输出**：
```sql
==>  Preparing: INSERT INTO user ( username, email, age, create_time, update_time ) 
                VALUES ( ?, ?, ?, ?, ? )
==> Parameters: 测试用户(String), test@example.com(String), 25(Integer), 
                2024-01-01T12:00:00(LocalDateTime), 2024-01-01T12:00:00(LocalDateTime)
```

### 4.3 测试更新

```java
@Test
public void testUpdate() {
    User user = userMapper.selectById(1L);
    user.setUsername("新名字");
    
    // 无需手动设置 updateTime
    userMapper.updateById(user);
    
    System.out.println("更新时间：" + user.getUpdateTime());
}
```

**输出**：
```sql
==>  Preparing: UPDATE user SET username=?, update_time=? WHERE id=? AND deleted=0
==> Parameters: 新名字(String), 2024-01-01T13:00:00(LocalDateTime), 1(Long)
```

---

## 5. 自动填充创建人和更新人

### 5.1 获取当前用户

```java
package com.example.utils;

import org.springframework.web.context.request.RequestContextHolder;
import org.springframework.web.context.request.ServletRequestAttributes;

import javax.servlet.http.HttpServletRequest;

public class UserContext {
    
    /**
     * 获取当前登录用户ID
     */
    public static Long getCurrentUserId() {
        ServletRequestAttributes attributes = 
            (ServletRequestAttributes) RequestContextHolder.getRequestAttributes();
        
        if (attributes == null) {
            return 1L;  // 默认用户ID（实际应从 Session/Token 获取）
        }
        
        HttpServletRequest request = attributes.getRequest();
        String userId = request.getHeader("X-User-Id");
        
        return userId != null ? Long.parseLong(userId) : 1L;
    }
    
    /**
     * 获取当前登录用户名
     */
    public static String getCurrentUsername() {
        // 从 Session/Token 获取
        return "admin";
    }
}
```

### 5.2 实体类

```java
@Data
@TableName("article")
public class Article {
    
    @TableId(type = IdType.AUTO)
    private Long id;
    
    private String title;
    
    private String content;
    
    @TableField(fill = FieldFill.INSERT)
    private Long createBy;
    
    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createTime;
    
    @TableField(fill = FieldFill.INSERT_UPDATE)
    private Long updateBy;
    
    @TableField(fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updateTime;
}
```

### 5.3 处理器

```java
@Slf4j
@Component
public class ArticleMetaObjectHandler implements MetaObjectHandler {
    
    @Override
    public void insertFill(MetaObject metaObject) {
        Long userId = UserContext.getCurrentUserId();
        LocalDateTime now = LocalDateTime.now();
        
        this.fillStrategy(metaObject, "createBy", userId);
        this.fillStrategy(metaObject, "createTime", now);
        this.fillStrategy(metaObject, "updateBy", userId);
        this.fillStrategy(metaObject, "updateTime", now);
    }
    
    @Override
    public void updateFill(MetaObject metaObject) {
        Long userId = UserContext.getCurrentUserId();
        LocalDateTime now = LocalDateTime.now();
        
        this.fillStrategy(metaObject, "updateBy", userId);
        this.fillStrategy(metaObject, "updateTime", now);
    }
}
```

---

## 6. 条件填充

### 6.1 只在字段为空时填充

```java
@Override
public void insertFill(MetaObject metaObject) {
    // 只有 createTime 为 null 时才填充
    Object createTime = getFieldValByName("createTime", metaObject);
    if (createTime == null) {
        this.fillStrategy(metaObject, "createTime", LocalDateTime.now());
    }
}
```

### 6.2 根据条件填充不同值

```java
@Override
public void insertFill(MetaObject metaObject) {
    // 根据状态设置不同的初始值
    Object status = getFieldValByName("status", metaObject);
    if (status == null) {
        this.fillStrategy(metaObject, "status", 0);  // 默认待审核
    }
}
```

---

## 7. 自定义类型处理器

### 7.1 场景：JSON 字段

数据库存储 JSON 字符串，Java 使用对象。

#### 实体类

```java
@Data
@TableName("product")
public class Product {
    
    @TableId
    private Long id;
    
    private String name;
    
    @TableField(typeHandler = JacksonTypeHandler.class)
    private ProductSpec spec;  // 商品规格（JSON）
}

@Data
public class ProductSpec {
    private String color;
    private String size;
    private List<String> tags;
}
```

#### 配置

```yaml
mybatis-plus:
  type-handlers-package: com.baomidou.mybatisplus.extension.handlers
```

### 7.2 场景：加密字段

```java
@Component
public class EncryptTypeHandler implements TypeHandler<String> {
    
    @Override
    public void setParameter(PreparedStatement ps, int i, 
                            String parameter, JdbcType jdbcType) 
                            throws SQLException {
        ps.setString(i, encrypt(parameter));
    }
    
    @Override
    public String getResult(ResultSet rs, String columnName) 
                           throws SQLException {
        return decrypt(rs.getString(columnName));
    }
    
    private String encrypt(String plainText) {
        // 加密逻辑
        return Base64.getEncoder().encodeToString(plainText.getBytes());
    }
    
    private String decrypt(String encryptedText) {
        // 解密逻辑
        return new String(Base64.getDecoder().decode(encryptedText));
    }
}
```

---

## 8. 常见问题

### 8.1 自动填充不生效

**原因**：
* 未添加 `@Component` 注解
* 未配置 `fill` 属性
* 字段名不匹配

**解决**：
```java
// ✅ 确保添加了 @Component
@Component
public class MyMetaObjectHandler implements MetaObjectHandler {
    // ...
}

// ✅ 实体类配置 fill
@TableField(fill = FieldFill.INSERT)
private LocalDateTime createTime;
```

### 8.2 字段名大小写问题

**问题**：驼峰命名和下划线命名不匹配

**解决**：开启驼峰映射
```yaml
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
```

### 8.3 性能问题

**问题**：每次操作都执行填充逻辑

**优化**：
```java
@Override
public void insertFill(MetaObject metaObject) {
    // 先判断是否需要填充
    if (getFieldValByName("createTime", metaObject) == null) {
        this.fillStrategy(metaObject, "createTime", LocalDateTime.now());
    }
}
```

---

## 9. 实战练习

### 练习 1：博客文章

为博客文章表添加：
* 创建时间、更新时间自动填充
* 创建人、更新人自动填充
* 阅读数默认为 0

### 练习 2：订单系统

为订单表添加：
* 订单号自动生成（ORDER + 时间戳）
* 订单状态默认为"待支付"
* 下单时间自动填充

### 练习 3：审计日志

实现完整的审计功能：
* 记录所有操作的创建人、更新人
* 记录操作时间
* 记录 IP 地址

---

## 10. 总结

### 10.1 核心知识点

✅ **@TableField**：字段映射、填充策略  
✅ **FieldFill**：INSERT、UPDATE、INSERT_UPDATE  
✅ **MetaObjectHandler**：insertFill、updateFill  
✅ **fillStrategy**：普通填充策略  
✅ **类型处理器**：JSON、加密等  

### 10.2 最佳实践

📌 优先使用 fillStrategy 而非 strictFill  
📌 填充前判断字段是否为空  
📌 时间字段使用 LocalDateTime  
📌 创建人/更新人从上下文获取  
📌 复杂类型使用 TypeHandler  

---

## 11. 下一步学习

📖 [06.乐观锁与主键策略](../06.乐观锁与主键策略.md)  
📖 [07.逻辑删除与数据权限](../07.逻辑删除与数据权限.md)  
📖 [08.高级查询功能](../08.高级查询功能.md)

---

**参考资料：**
* MyBatis-Plus 官方文档 - 自动填充：https://baomidou.com/pages/4c6bcf/
